# import * from './generated/prisma-client/prisma.graphql'

type Query {
  items: [Item]!
}

type Mutation {
  createItem(
    title: String
    description: String
    price: Float
    image: String
    largeImage: String
    createdAt: DateTime
    updatedAt: DateTime
  ): Item!
}
