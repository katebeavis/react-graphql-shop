endpoint: ${env:PRISMA_URL}
datamodel: datamodel.prisma

generate:
  - generator: typescript-client
    output: ./src/prisma-client/
  - generator: graphql-schema
    output: ./src/prisma-client/prisma.graphql

hooks:
  post-deploy:
    - graphql get-schema -p prisma
