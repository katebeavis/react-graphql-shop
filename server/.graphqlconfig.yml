projects:
  app:
    schemaPath: './src/schema.graphql'
    extensions:
      endpoints:
        default: 'http://localhost:4444'
  prisma:
    schemaPath: 'src/prisma-client/prisma.graphql'
    extensions:
      prisma: prisma.yml
